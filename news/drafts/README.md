# skynet-news drafts

Сюда S.scribe (TASK-33) кладёт готовые посты в формате news-codex §5:
`status: ready`, `target: test|@libmdbx`, длина <=400 (§8).
Конвейер (publisher.py) публикует `ready` и помечает `posted`.
Пример в `/sourcecraft/workspace/.skynet/news/drafts/` (вне git).
